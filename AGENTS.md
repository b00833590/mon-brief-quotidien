# mon-brief-quotidien

Voir `CLAUDE.md` — c'est la seule source de vérité pour l'architecture, le pipeline, la stack
technique et les points de fragilité connus de ce dépôt.

Ce fichier existe uniquement parce que certains outils cherchent `AGENTS.md` par convention. Son
contenu était auparavant dupliqué manuellement à partir de `CLAUDE.md` à chaque session — ça a
déjà dérivé une fois (une correction Netlify → Vercel oubliée dans un seul des deux fichiers). Ne
pas recopier de contenu ici : modifier `CLAUDE.md`, ce fichier n'a rien à maintenir.
